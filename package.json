{
  "private": true,
  "scripts": {
    "start": "rm -rf target && mkdir target && babel --presets stage-3,env --out-dir ./target ./src/**/*.js && node ./entry.js"
  },
  "dependencies": {
    "control.monads": "^0.6.0",
    "data.either": "^1.5.0",
    "denodeify": "^1.2.1",
    "express": "^4.13.3",
    "express-session": "^1.13.0",
    "lodash": "^4.17.4",
    "node-fetch": "^1.5.0"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-env": "^1.1.8",
    "babel-preset-stage-3": "^6.22.0"
  }
}
