{
  "private": true,
  "scripts": {
    "compile": "rm -rf target && mkdir target && babel --source-maps --presets stage-3,env --out-dir ./target ./{tests,src}/**/*.js",
    "start": " yarn compile && node --debug ./entry.js",
    "test": " yarn compile && node --debug ./entry-tests.js"
  },
  "dependencies": {
    "data.validation": "^1.3.1",
    "date-fns": "^1.27.2",
    "denodeify": "^1.2.1",
    "express": "^4.13.3",
    "express-session": "^1.13.0",
    "lodash": "^4.17.4",
    "node-fetch": "^1.5.0"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-env": "^1.1.8",
    "babel-preset-stage-3": "^6.22.0",
    "blue-tape": "^1.0.0"
  }
}
